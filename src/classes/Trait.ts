export class Trait {
    name: string;
    icon: string;
    description: string;

    constructor(name: string, icon: string, description: string) {
        this.name = name;
        this.icon = icon;
        this.description = description;
    }
}
